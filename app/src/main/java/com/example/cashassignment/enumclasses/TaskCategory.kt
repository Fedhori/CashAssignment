package com.example.cashassignment.enumclasses

/*
* fragment_mission.xml에 있는 tabLayout_missionFragment의 item 나열 순서는 이 enum class의 나열 순서와 같다.
*/
enum class TaskCategory {
    ALL, BOX, PICTURE, SURVEY, VALID
}